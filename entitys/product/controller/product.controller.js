const Product = require('../Product.Schema');

const productController = {};

productController.creteOrder = async (req, res, next) => {
  try {
  } catch {}
  next();
};

module.exports = productController;
